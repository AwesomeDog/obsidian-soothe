{
	"name": "obsidian-soothe",
	"version": "1.0.0",
	"description": "Tinge Obsidian with soothing colors, respect everything else Obsidian has to offer.",
	"license": "MIT",
	"private": true,
	"scripts": {
		"deploy": "node scripts/deploy.mjs",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	}
}
